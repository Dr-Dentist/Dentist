/* Cal.com booking links — the path after cal.com/ for each event type,
   e.g. "dr-asim-prakash/clinic-visit"  (no "https://", no "cal.com/").
   Leave a value "" and that option falls back to WhatsApp + phone. */
var CAL_LINK_CLINIC = "";   // in-person appointment at the clinic
var CAL_LINK_VIDEO  = "";   // video-call consultation
